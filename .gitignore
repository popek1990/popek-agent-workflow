# Plan files are project-specific, not for this repo
# (they live in target projects, not here)

.DS_Store
*.swp
*~

# Local agent instructions — not tracked in git
CLAUDE.md

# Local diagnostic scripts — not installed into target projects
scripts/*
# Wyjątek: notify.sh jest częścią template'u (kopiowany przez install.sh).
!scripts/notify.sh
# Wyjątki: narzędzia diagnostyczne repo workflow.
!scripts/test-repo.sh
!scripts/review_prompt.md
!scripts/extract-logs.sh
!scripts/extract_claude.py
!scripts/extract_gemini.py
!scripts/sokol-log.sh
!scripts/sokol-claude-log.sh
