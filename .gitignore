# Plan files are project-specific, not for this repo
# (they live in target projects, not here)

.DS_Store
*.swp
*~

# Local agent instructions — not tracked in git
CLAUDE.md

# Local diagnostic scripts — not part of the template
scripts/*
# Wyjątek: notify.sh jest częścią template'u (kopiowany przez install.sh)
!scripts/notify.sh
